---
date: 2026-05-28
tags: [日报]
status: in-progress
retrieval_triggers: [2026-05-28]
---

## 今日任务

### base/user · hk 分支

- [x] **短信服务替换**：`rw-notification-client`（NotifyServiceImpl）→ `rw-common-sms`（SmsNotifierFactory + SmsNotifierImpl）
  - 配置项从 `notify.internal.service.url` 改为腾讯/云片双渠道：`tencent.sms.appid/appkey`、`yunpian.api.key`
  - UserApi 注入类型从 `NotifyService` 改为 `SmsNotifier`
- [x] **Ticket IP 白名单**：AbstractTicketManager 新增 `isIpWhitelisted()`，双检锁懒加载，默认 IP `61.49.177.138,61.148.40.10`，配置项 `ticket.ip.whitelist`
- [x] **前端密码加密**：引入 jsencrypt 3.1.0 webjar，所有登录页（V1-V5）密码前端加密后传输
- [x] **新增 LoginV5Page**：hk 专用新登录页
- [x] **引入 scene dubbo 服务**（替换原 Hessian webservice）
- [x] **[base/user · 15:44]** `-` feture:替换webservice
- [x] **[base/user · 15:35]** `-` feture:替换webservice
- [x] **[base/user · 14:03]** `-` feture:替换webservice
- [x] **[base/user · 13:30]** `-` feture:替换webservice
- [x] **[base/user · 11:18]** `-` feture:引入scene dubbo服务
- [x] **[base/scene · 16:20]** `feature` iccid同步
- [x] **[manager/rw-backend · 16:43]** `feature` iccid信息不全
- [x] **[manager/rw-backend · 11:48]** `-` 依赖版本升级
- [x] **[manager/rw-backend · 11:42]** `-` 演示环境加账号限制
- [x] **[manager/rw-backend · 16:54]** `chore` 忽略 .gstack 本地工具目录
- [x] **[manager/rw-backend · 19:18]** `fix` 定时任务补全流量卡后改为写入去重标记而非清除
	- clearDedup 删除 Redis 去重 key 会导致 Kafka 在定时任务成功后立即
	- 重复触发供应商查询；改为 markDedup 写入相同 key（TTL 1天），
	- 与 Kafka 监听保持语义一致，避免冗余的供应商 API 调用。
	- clearDedup 删除 Redis 去重 key 会导致 Kafka 在定时任务成功后立即
	- 重复触发供应商查询；改为 markDedup 写入相同 key（TTL 1天），
	- 与 Kafka 监听保持语义一致，避免冗余的供应商 API 调用。
	- clearDedup 删除 Redis 去重 key 会导致 Kafka 在定时任务成功后立即
	- 重复触发供应商查询；改为 markDedup 写入相同 key（TTL 1天），
	- 与 Kafka 监听保持语义一致，避免冗余的供应商 API 调用。
	- clearDedup 删除 Redis 去重 key 会导致 Kafka 在定时任务成功后立即
	- 重复触发供应商查询；改为 markDedup 写入相同 key（TTL 1天），
	- 与 Kafka 监听保持语义一致，避免冗余的供应商 API 调用。
	- clearDedup 删除 Redis 去重 key 会导致 Kafka 在定时任务成功后立即
	- 重复触发供应商查询；改为 markDedup 写入相同 key（TTL 1天），
	- 与 Kafka 监听保持语义一致，避免冗余的供应商 API 调用。
	- clearDedup 删除 Redis 去重 key 会导致 Kafka 在定时任务成功后立即
	- 重复触发供应商查询；改为 markDedup 写入相同 key（TTL 1天），
	- 与 Kafka 监听保持语义一致，避免冗余的供应商 API 调用。
	- clearDedup 删除 Redis 去重 key 会导致 Kafka 在定时任务成功后立即
	- 重复触发供应商查询；改为 markDedup 写入相同 key（TTL 1天），
	- 与 Kafka 监听保持语义一致，避免冗余的供应商 API 调用。
	- clearDedup 删除 Redis 去重 key 会导致 Kafka 在定时任务成功后立即
	- 重复触发供应商查询；改为 markDedup 写入相同 key（TTL 1天），
	- 与 Kafka 监听保持语义一致，避免冗余的供应商 API 调用。
	- clearDedup 删除 Redis 去重 key 会导致 Kafka 在定时任务成功后立即
	- 重复触发供应商查询；改为 markDedup 写入相同 key（TTL 1天），
	- 与 Kafka 监听保持语义一致，避免冗余的供应商 API 调用。
	- clearDedup 删除 Redis 去重 key 会导致 Kafka 在定时任务成功后立即
	- 重复触发供应商查询；改为 markDedup 写入相同 key（TTL 1天），
	- 与 Kafka 监听保持语义一致，避免冗余的供应商 API 调用。
	- clearDedup 删除 Redis 去重 key 会导致 Kafka 在定时任务成功后立即
	- 重复触发供应商查询；改为 markDedup 写入相同 key（TTL 1天），
	- 与 Kafka 监听保持语义一致，避免冗余的供应商 API 调用。
	- clearDedup 删除 Redis 去重 key 会导致 Kafka 在定时任务成功后立即
	- 重复触发供应商查询；改为 markDedup 写入相同 key（TTL 1天），
	- 与 Kafka 监听保持语义一致，避免冗余的供应商 API 调用。
	- clearDedup 删除 Redis 去重 key 会导致 Kafka 在定时任务成功后立即
	- 重复触发供应商查询；改为 markDedup 写入相同 key（TTL 1天），
	- 与 Kafka 监听保持语义一致，避免冗余的供应商 API 调用。
	- clearDedup 删除 Redis 去重 key 会导致 Kafka 在定时任务成功后立即
	- 重复触发供应商查询；改为 markDedup 写入相同 key（TTL 1天），
	- 与 Kafka 监听保持语义一致，避免冗余的供应商 API 调用。
	- clearDedup 删除 Redis 去重 key 会导致 Kafka 在定时任务成功后立即
	- 重复触发供应商查询；改为 markDedup 写入相同 key（TTL 1天），
	- 与 Kafka 监听保持语义一致，避免冗余的供应商 API 调用。
- [x] **[L2/l2-orchestrator · 19:45]** `feat` 任务24/25 - API-04 设备执行结果回调 + Topic 重命名
	- - 新增 POST /api/v1/mes/device/callback 接口，接收 TCP 代理设备执行结果，推送到 Kafka
	- - 新增 MesDeviceCallbackRequest DTO、MesDeviceResultProducer、receive_device_result() 方法
	- - Topic 重命名：mes_agv_callback → mes.agv.command，mes.device.result → mes.agv.result
	- - 补充 TestMesServiceDeviceResult 6 个测试用例，21 passed
	- clearDedup 删除 Redis 去重 key 会导致 Kafka 在定时任务成功后立即
	- 重复触发供应商查询；改为 markDedup 写入相同 key（TTL 1天），
	- 与 Kafka 监听保持语义一致，避免冗余的供应商 API 调用。
	- - 新增 POST /api/v1/mes/device/callback 接口，接收 TCP 代理设备执行结果，推送到 Kafka
	- - 新增 MesDeviceCallbackRequest DTO、MesDeviceResultProducer、receive_device_result() 方法
	- - Topic 重命名：mes_agv_callback → mes.agv.command，mes.device.result → mes.agv.result
	- - 补充 TestMesServiceDeviceResult 6 个测试用例，21 passed
	- clearDedup 删除 Redis 去重 key 会导致 Kafka 在定时任务成功后立即
	- 重复触发供应商查询；改为 markDedup 写入相同 key（TTL 1天），
	- 与 Kafka 监听保持语义一致，避免冗余的供应商 API 调用。
	- - 新增 POST /api/v1/mes/device/callback 接口，接收 TCP 代理设备执行结果，推送到 Kafka
	- - 新增 MesDeviceCallbackRequest DTO、MesDeviceResultProducer、receive_device_result() 方法
	- - Topic 重命名：mes_agv_callback → mes.agv.command，mes.device.result → mes.agv.result
	- - 补充 TestMesServiceDeviceResult 6 个测试用例，21 passed
	- clearDedup 删除 Redis 去重 key 会导致 Kafka 在定时任务成功后立即
	- 重复触发供应商查询；改为 markDedup 写入相同 key（TTL 1天），
	- 与 Kafka 监听保持语义一致，避免冗余的供应商 API 调用。
- [x] **[L2/l2-orchestrator · 19:48]** `refactor` 路由路径规范化 /mes/agv/command + /mes/agv/callback
	- - /mes/command → /mes/agv/command
	- - /mes/device/callback → /mes/agv/callback
	- - 文档同步更新
	- - 新增 POST /api/v1/mes/device/callback 接口，接收 TCP 代理设备执行结果，推送到 Kafka
	- - 新增 MesDeviceCallbackRequest DTO、MesDeviceResultProducer、receive_device_result() 方法
	- - Topic 重命名：mes_agv_callback → mes.agv.command，mes.device.result → mes.agv.result
	- - 补充 TestMesServiceDeviceResult 6 个测试用例，21 passed
	- clearDedup 删除 Redis 去重 key 会导致 Kafka 在定时任务成功后立即
	- 重复触发供应商查询；改为 markDedup 写入相同 key（TTL 1天），
	- 与 Kafka 监听保持语义一致，避免冗余的供应商 API 调用。
	- - /mes/command → /mes/agv/command
	- - /mes/device/callback → /mes/agv/callback
	- - 文档同步更新
	- - 新增 POST /api/v1/mes/device/callback 接口，接收 TCP 代理设备执行结果，推送到 Kafka
	- - 新增 MesDeviceCallbackRequest DTO、MesDeviceResultProducer、receive_device_result() 方法
	- - Topic 重命名：mes_agv_callback → mes.agv.command，mes.device.result → mes.agv.result
	- - 补充 TestMesServiceDeviceResult 6 个测试用例，21 passed
	- clearDedup 删除 Redis 去重 key 会导致 Kafka 在定时任务成功后立即
	- 重复触发供应商查询；改为 markDedup 写入相同 key（TTL 1天），
	- 与 Kafka 监听保持语义一致，避免冗余的供应商 API 调用。
	- - /mes/command → /mes/agv/command
	- - /mes/device/callback → /mes/agv/callback
	- - 文档同步更新
	- - 新增 POST /api/v1/mes/device/callback 接口，接收 TCP 代理设备执行结果，推送到 Kafka
	- - 新增 MesDeviceCallbackRequest DTO、MesDeviceResultProducer、receive_device_result() 方法
	- - Topic 重命名：mes_agv_callback → mes.agv.command，mes.device.result → mes.agv.result
	- - 补充 TestMesServiceDeviceResult 6 个测试用例，21 passed
	- clearDedup 删除 Redis 去重 key 会导致 Kafka 在定时任务成功后立即
	- 重复触发供应商查询；改为 markDedup 写入相同 key（TTL 1天），
	- 与 Kafka 监听保持语义一致，避免冗余的供应商 API 调用。
	- - /mes/command → /mes/agv/command
	- - /mes/device/callback → /mes/agv/callback
	- - 文档同步更新
	- - 新增 POST /api/v1/mes/device/callback 接口，接收 TCP 代理设备执行结果，推送到 Kafka
	- - 新增 MesDeviceCallbackRequest DTO、MesDeviceResultProducer、receive_device_result() 方法
	- - Topic 重命名：mes_agv_callback → mes.agv.command，mes.device.result → mes.agv.result
	- - 补充 TestMesServiceDeviceResult 6 个测试用例，21 passed
	- clearDedup 删除 Redis 去重 key 会导致 Kafka 在定时任务成功后立即
	- 重复触发供应商查询；改为 markDedup 写入相同 key（TTL 1天），
	- 与 Kafka 监听保持语义一致，避免冗余的供应商 API 调用。
	- - /mes/command → /mes/agv/command
	- - /mes/device/callback → /mes/agv/callback
	- - 文档同步更新
	- - 新增 POST /api/v1/mes/device/callback 接口，接收 TCP 代理设备执行结果，推送到 Kafka
	- - 新增 MesDeviceCallbackRequest DTO、MesDeviceResultProducer、receive_device_result() 方法
	- - Topic 重命名：mes_agv_callback → mes.agv.command，mes.device.result → mes.agv.result
	- - 补充 TestMesServiceDeviceResult 6 个测试用例，21 passed
	- clearDedup 删除 Redis 去重 key 会导致 Kafka 在定时任务成功后立即
	- 重复触发供应商查询；改为 markDedup 写入相同 key（TTL 1天），
	- 与 Kafka 监听保持语义一致，避免冗余的供应商 API 调用。
	- - /mes/command → /mes/agv/command
	- - /mes/device/callback → /mes/agv/callback
	- - 文档同步更新
	- - 新增 POST /api/v1/mes/device/callback 接口，接收 TCP 代理设备执行结果，推送到 Kafka
	- - 新增 MesDeviceCallbackRequest DTO、MesDeviceResultProducer、receive_device_result() 方法
	- - Topic 重命名：mes_agv_callback → mes.agv.command，mes.device.result → mes.agv.result
	- - 补充 TestMesServiceDeviceResult 6 个测试用例，21 passed
	- clearDedup 删除 Redis 去重 key 会导致 Kafka 在定时任务成功后立即
	- 重复触发供应商查询；改为 markDedup 写入相同 key（TTL 1天），
	- 与 Kafka 监听保持语义一致，避免冗余的供应商 API 调用。
	- - /mes/command → /mes/agv/command
	- - /mes/device/callback → /mes/agv/callback
	- - 文档同步更新
	- - 新增 POST /api/v1/mes/device/callback 接口，接收 TCP 代理设备执行结果，推送到 Kafka
	- - 新增 MesDeviceCallbackRequest DTO、MesDeviceResultProducer、receive_device_result() 方法
	- - Topic 重命名：mes_agv_callback → mes.agv.command，mes.device.result → mes.agv.result
	- - 补充 TestMesServiceDeviceResult 6 个测试用例，21 passed
	- clearDedup 删除 Redis 去重 key 会导致 Kafka 在定时任务成功后立即
	- 重复触发供应商查询；改为 markDedup 写入相同 key（TTL 1天），
	- 与 Kafka 监听保持语义一致，避免冗余的供应商 API 调用。
	- - /mes/command → /mes/agv/command
	- - /mes/device/callback → /mes/agv/callback
	- - 文档同步更新
	- - 新增 POST /api/v1/mes/device/callback 接口，接收 TCP 代理设备执行结果，推送到 Kafka
	- - 新增 MesDeviceCallbackRequest DTO、MesDeviceResultProducer、receive_device_result() 方法
	- - Topic 重命名：mes_agv_callback → mes.agv.command，mes.device.result → mes.agv.result
	- - 补充 TestMesServiceDeviceResult 6 个测试用例，21 passed
	- clearDedup 删除 Redis 去重 key 会导致 Kafka 在定时任务成功后立即
	- 重复触发供应商查询；改为 markDedup 写入相同 key（TTL 1天），
	- 与 Kafka 监听保持语义一致，避免冗余的供应商 API 调用。


### l2-orchestrator · dev_gaowei_branch

- [x] **API-04 设备执行结果回调**：新增 `POST /api/v1/mes/agv/callback`，接收 TCP 代理推送的 AGV 设备执行结果，转发到 Kafka topic `mes.agv.result`
  - 新增 `MesDeviceCallbackRequest` DTO、`MesDeviceResultProducer`、`MesService.receive_device_result()`
  - 补充 `TestMesServiceDeviceResult` 6 个单元测试，21 passed
- [x] **MES Kafka topic 重命名**：统一点号风格并修正语义
  - `mes_agv_callback` → `mes.agv.command`（下发指令，非回调）
  - `mes.device.result` → `mes.agv.result`（与 command 对称）
- [x] **路由规范化**：`/mes/command` → `/mes/agv/command`，`/mes/device/callback` → `/mes/agv/callback`
- [x] **文档更新**：需求文档、开发文档追加任务24（API-04）、任务25（topic 重命名）
	- - /mes/command → /mes/agv/command
	- - /mes/device/callback → /mes/agv/callback
	- - 文档同步更新
	- - 新增 POST /api/v1/mes/device/callback 接口，接收 TCP 代理设备执行结果，推送到 Kafka
	- - 新增 MesDeviceCallbackRequest DTO、MesDeviceResultProducer、receive_device_result() 方法
	- - Topic 重命名：mes_agv_callback → mes.agv.command，mes.device.result → mes.agv.result
	- - 补充 TestMesServiceDeviceResult 6 个测试用例，21 passed
	- clearDedup 删除 Redis 去重 key 会导致 Kafka 在定时任务成功后立即
	- 重复触发供应商查询；改为 markDedup 写入相同 key（TTL 1天），
	- 与 Kafka 监听保持语义一致，避免冗余的供应商 API 调用。
	- - /mes/command → /mes/agv/command
	- - /mes/device/callback → /mes/agv/callback
	- - 文档同步更新
	- - 新增 POST /api/v1/mes/device/callback 接口，接收 TCP 代理设备执行结果，推送到 Kafka
	- - 新增 MesDeviceCallbackRequest DTO、MesDeviceResultProducer、receive_device_result() 方法
	- - Topic 重命名：mes_agv_callback → mes.agv.command，mes.device.result → mes.agv.result
	- - 补充 TestMesServiceDeviceResult 6 个测试用例，21 passed
	- clearDedup 删除 Redis 去重 key 会导致 Kafka 在定时任务成功后立即
	- 重复触发供应商查询；改为 markDedup 写入相同 key（TTL 1天），
	- 与 Kafka 监听保持语义一致，避免冗余的供应商 API 调用。
	- - /mes/command → /mes/agv/command
	- - /mes/device/callback → /mes/agv/callback
	- - 文档同步更新
	- - 新增 POST /api/v1/mes/device/callback 接口，接收 TCP 代理设备执行结果，推送到 Kafka
	- - 新增 MesDeviceCallbackRequest DTO、MesDeviceResultProducer、receive_device_result() 方法
	- - Topic 重命名：mes_agv_callback → mes.agv.command，mes.device.result → mes.agv.result
	- - 补充 TestMesServiceDeviceResult 6 个测试用例，21 passed
	- clearDedup 删除 Redis 去重 key 会导致 Kafka 在定时任务成功后立即
	- 重复触发供应商查询；改为 markDedup 写入相同 key（TTL 1天），
	- 与 Kafka 监听保持语义一致，避免冗余的供应商 API 调用。
	- - /mes/command → /mes/agv/command
	- - /mes/device/callback → /mes/agv/callback
	- - 文档同步更新
	- - 新增 POST /api/v1/mes/device/callback 接口，接收 TCP 代理设备执行结果，推送到 Kafka
	- - 新增 MesDeviceCallbackRequest DTO、MesDeviceResultProducer、receive_device_result() 方法
	- - Topic 重命名：mes_agv_callback → mes.agv.command，mes.device.result → mes.agv.result
	- - 补充 TestMesServiceDeviceResult 6 个测试用例，21 passed
	- clearDedup 删除 Redis 去重 key 会导致 Kafka 在定时任务成功后立即
	- 重复触发供应商查询；改为 markDedup 写入相同 key（TTL 1天），
	- 与 Kafka 监听保持语义一致，避免冗余的供应商 API 调用。
	- - /mes/command → /mes/agv/command
	- - /mes/device/callback → /mes/agv/callback
	- - 文档同步更新
	- - 新增 POST /api/v1/mes/device/callback 接口，接收 TCP 代理设备执行结果，推送到 Kafka
	- - 新增 MesDeviceCallbackRequest DTO、MesDeviceResultProducer、receive_device_result() 方法
	- - Topic 重命名：mes_agv_callback → mes.agv.command，mes.device.result → mes.agv.result
	- - 补充 TestMesServiceDeviceResult 6 个测试用例，21 passed
	- clearDedup 删除 Redis 去重 key 会导致 Kafka 在定时任务成功后立即
	- 重复触发供应商查询；改为 markDedup 写入相同 key（TTL 1天），
	- 与 Kafka 监听保持语义一致，避免冗余的供应商 API 调用。
	- - /mes/command → /mes/agv/command
	- - /mes/device/callback → /mes/agv/callback
	- - 文档同步更新
	- - 新增 POST /api/v1/mes/device/callback 接口，接收 TCP 代理设备执行结果，推送到 Kafka
	- - 新增 MesDeviceCallbackRequest DTO、MesDeviceResultProducer、receive_device_result() 方法
	- - Topic 重命名：mes_agv_callback → mes.agv.command，mes.device.result → mes.agv.result
	- - 补充 TestMesServiceDeviceResult 6 个测试用例，21 passed
	- clearDedup 删除 Redis 去重 key 会导致 Kafka 在定时任务成功后立即
	- 重复触发供应商查询；改为 markDedup 写入相同 key（TTL 1天），
	- 与 Kafka 监听保持语义一致，避免冗余的供应商 API 调用。
	- - /mes/command → /mes/agv/command
	- - /mes/device/callback → /mes/agv/callback
	- - 文档同步更新
	- - 新增 POST /api/v1/mes/device/callback 接口，接收 TCP 代理设备执行结果，推送到 Kafka
	- - 新增 MesDeviceCallbackRequest DTO、MesDeviceResultProducer、receive_device_result() 方法
	- - Topic 重命名：mes_agv_callback → mes.agv.command，mes.device.result → mes.agv.result
	- - 补充 TestMesServiceDeviceResult 6 个测试用例，21 passed
	- clearDedup 删除 Redis 去重 key 会导致 Kafka 在定时任务成功后立即
	- 重复触发供应商查询；改为 markDedup 写入相同 key（TTL 1天），
	- 与 Kafka 监听保持语义一致，避免冗余的供应商 API 调用。
	- - /mes/command → /mes/agv/command
	- - /mes/device/callback → /mes/agv/callback
	- - 文档同步更新
	- - 新增 POST /api/v1/mes/device/callback 接口，接收 TCP 代理设备执行结果，推送到 Kafka
	- - 新增 MesDeviceCallbackRequest DTO、MesDeviceResultProducer、receive_device_result() 方法
	- - Topic 重命名：mes_agv_callback → mes.agv.command，mes.device.result → mes.agv.result
	- - 补充 TestMesServiceDeviceResult 6 个测试用例，21 passed
	- clearDedup 删除 Redis 去重 key 会导致 Kafka 在定时任务成功后立即
	- 重复触发供应商查询；改为 markDedup 写入相同 key（TTL 1天），
	- 与 Kafka 监听保持语义一致，避免冗余的供应商 API 调用。
	- - /mes/command → /mes/agv/command
	- - /mes/device/callback → /mes/agv/callback
	- - 文档同步更新
	- - 新增 POST /api/v1/mes/device/callback 接口，接收 TCP 代理设备执行结果，推送到 Kafka
	- - 新增 MesDeviceCallbackRequest DTO、MesDeviceResultProducer、receive_device_result() 方法
	- - Topic 重命名：mes_agv_callback → mes.agv.command，mes.device.result → mes.agv.result
	- - 补充 TestMesServiceDeviceResult 6 个测试用例，21 passed
	- clearDedup 删除 Redis 去重 key 会导致 Kafka 在定时任务成功后立即
	- 重复触发供应商查询；改为 markDedup 写入相同 key（TTL 1天），
	- 与 Kafka 监听保持语义一致，避免冗余的供应商 API 调用。
	- - /mes/command → /mes/agv/command
	- - /mes/device/callback → /mes/agv/callback
	- - 文档同步更新
	- - 新增 POST /api/v1/mes/device/callback 接口，接收 TCP 代理设备执行结果，推送到 Kafka
	- - 新增 MesDeviceCallbackRequest DTO、MesDeviceResultProducer、receive_device_result() 方法
	- - Topic 重命名：mes_agv_callback → mes.agv.command，mes.device.result → mes.agv.result
	- - 补充 TestMesServiceDeviceResult 6 个测试用例，21 passed
	- clearDedup 删除 Redis 去重 key 会导致 Kafka 在定时任务成功后立即
	- 重复触发供应商查询；改为 markDedup 写入相同 key（TTL 1天），
	- 与 Kafka 监听保持语义一致，避免冗余的供应商 API 调用。
	- - /mes/command → /mes/agv/command
	- - /mes/device/callback → /mes/agv/callback
	- - 文档同步更新
	- - 新增 POST /api/v1/mes/device/callback 接口，接收 TCP 代理设备执行结果，推送到 Kafka
	- - 新增 MesDeviceCallbackRequest DTO、MesDeviceResultProducer、receive_device_result() 方法
	- - Topic 重命名：mes_agv_callback → mes.agv.command，mes.device.result → mes.agv.result
	- - 补充 TestMesServiceDeviceResult 6 个测试用例，21 passed
	- clearDedup 删除 Redis 去重 key 会导致 Kafka 在定时任务成功后立即
	- 重复触发供应商查询；改为 markDedup 写入相同 key（TTL 1天），
	- 与 Kafka 监听保持语义一致，避免冗余的供应商 API 调用。
	- - /mes/command → /mes/agv/command
	- - /mes/device/callback → /mes/agv/callback
	- - 文档同步更新
	- - 新增 POST /api/v1/mes/device/callback 接口，接收 TCP 代理设备执行结果，推送到 Kafka
	- - 新增 MesDeviceCallbackRequest DTO、MesDeviceResultProducer、receive_device_result() 方法
	- - Topic 重命名：mes_agv_callback → mes.agv.command，mes.device.result → mes.agv.result
	- - 补充 TestMesServiceDeviceResult 6 个测试用例，21 passed
	- clearDedup 删除 Redis 去重 key 会导致 Kafka 在定时任务成功后立即
	- 重复触发供应商查询；改为 markDedup 写入相同 key（TTL 1天），
	- 与 Kafka 监听保持语义一致，避免冗余的供应商 API 调用。
	- - /mes/command → /mes/agv/command
	- - /mes/device/callback → /mes/agv/callback
	- - 文档同步更新
	- - 新增 POST /api/v1/mes/device/callback 接口，接收 TCP 代理设备执行结果，推送到 Kafka
	- - 新增 MesDeviceCallbackRequest DTO、MesDeviceResultProducer、receive_device_result() 方法
	- - Topic 重命名：mes_agv_callback → mes.agv.command，mes.device.result → mes.agv.result
	- - 补充 TestMesServiceDeviceResult 6 个测试用例，21 passed
	- clearDedup 删除 Redis 去重 key 会导致 Kafka 在定时任务成功后立即
	- 重复触发供应商查询；改为 markDedup 写入相同 key（TTL 1天），
	- 与 Kafka 监听保持语义一致，避免冗余的供应商 API 调用。
	- - /mes/command → /mes/agv/command
	- - /mes/device/callback → /mes/agv/callback
	- - 文档同步更新
	- - 新增 POST /api/v1/mes/device/callback 接口，接收 TCP 代理设备执行结果，推送到 Kafka
	- - 新增 MesDeviceCallbackRequest DTO、MesDeviceResultProducer、receive_device_result() 方法
	- - Topic 重命名：mes_agv_callback → mes.agv.command，mes.device.result → mes.agv.result
	- - 补充 TestMesServiceDeviceResult 6 个测试用例，21 passed
	- clearDedup 删除 Redis 去重 key 会导致 Kafka 在定时任务成功后立即
	- 重复触发供应商查询；改为 markDedup 写入相同 key（TTL 1天），
	- 与 Kafka 监听保持语义一致，避免冗余的供应商 API 调用。
	- - /mes/command → /mes/agv/command
	- - /mes/device/callback → /mes/agv/callback
	- - 文档同步更新
	- - 新增 POST /api/v1/mes/device/callback 接口，接收 TCP 代理设备执行结果，推送到 Kafka
	- - 新增 MesDeviceCallbackRequest DTO、MesDeviceResultProducer、receive_device_result() 方法
	- - Topic 重命名：mes_agv_callback → mes.agv.command，mes.device.result → mes.agv.result
	- - 补充 TestMesServiceDeviceResult 6 个测试用例，21 passed
	- clearDedup 删除 Redis 去重 key 会导致 Kafka 在定时任务成功后立即
	- 重复触发供应商查询；改为 markDedup 写入相同 key（TTL 1天），
	- 与 Kafka 监听保持语义一致，避免冗余的供应商 API 调用。
	- - /mes/command → /mes/agv/command
	- - /mes/device/callback → /mes/agv/callback
	- - 文档同步更新
	- - 新增 POST /api/v1/mes/device/callback 接口，接收 TCP 代理设备执行结果，推送到 Kafka
	- - 新增 MesDeviceCallbackRequest DTO、MesDeviceResultProducer、receive_device_result() 方法
	- - Topic 重命名：mes_agv_callback → mes.agv.command，mes.device.result → mes.agv.result
	- - 补充 TestMesServiceDeviceResult 6 个测试用例，21 passed
	- clearDedup 删除 Redis 去重 key 会导致 Kafka 在定时任务成功后立即
	- 重复触发供应商查询；改为 markDedup 写入相同 key（TTL 1天），
	- 与 Kafka 监听保持语义一致，避免冗余的供应商 API 调用。
	- - /mes/command → /mes/agv/command
	- - /mes/device/callback → /mes/agv/callback
	- - 文档同步更新
	- - 新增 POST /api/v1/mes/device/callback 接口，接收 TCP 代理设备执行结果，推送到 Kafka
	- - 新增 MesDeviceCallbackRequest DTO、MesDeviceResultProducer、receive_device_result() 方法
	- - Topic 重命名：mes_agv_callback → mes.agv.command，mes.device.result → mes.agv.result
	- - 补充 TestMesServiceDeviceResult 6 个测试用例，21 passed
	- clearDedup 删除 Redis 去重 key 会导致 Kafka 在定时任务成功后立即
	- 重复触发供应商查询；改为 markDedup 写入相同 key（TTL 1天），
	- 与 Kafka 监听保持语义一致，避免冗余的供应商 API 调用。
	- - /mes/command → /mes/agv/command
	- - /mes/device/callback → /mes/agv/callback
	- - 文档同步更新
	- - 新增 POST /api/v1/mes/device/callback 接口，接收 TCP 代理设备执行结果，推送到 Kafka
	- - 新增 MesDeviceCallbackRequest DTO、MesDeviceResultProducer、receive_device_result() 方法
	- - Topic 重命名：mes_agv_callback → mes.agv.command，mes.device.result → mes.agv.result
	- - 补充 TestMesServiceDeviceResult 6 个测试用例，21 passed
	- clearDedup 删除 Redis 去重 key 会导致 Kafka 在定时任务成功后立即
	- 重复触发供应商查询；改为 markDedup 写入相同 key（TTL 1天），
	- 与 Kafka 监听保持语义一致，避免冗余的供应商 API 调用。

## 学到的东西

## 明日待做
# 今日总结

- **l2-orchestrator**：API-04 设备执行结果回调接口（`POST /mes/agv/callback` → Kafka `mes.agv.result`）、topic 重命名（`mes.agv.command` / `mes.agv.result`）、路由规范化、补测试 21 passed
