---
date: 2026-06-03
tags: [日报]
status: done
retrieval_triggers: [2026-06-03, iot-min, open-api, ubox, lot]
---

## 今日任务
- [x] **[iot-min/open-api · 接口移植]** `feature` 从 lot/open-api 移植两个查询接口
	- 前置分析：扫描 lot/open-api 中 4 个接口的存在性和可移植性
		- `POST /openapi/v1/store/device/query`：依赖 HDOS 外部服务（`resolvePlaceId`），iot-min 无此配置 → **暂不移植**
		- `POST /openapi/v1/place/list`：iot-min 已存在相同实现 → **跳过**
		- `GET /openapi/v1/robot/sn`：依赖 `sceneBaseService.getSnByProductId()`，iot-min OpenBaseApi 已注入 → **移植**
		- `GET /openapi/v2/place/marker/listAll`：依赖 `mapService.listMaps()` + `getRecentMapMarkers()`，iot-min common 均已实现 → **移植**
	- `OpenRobotInfoApi`：新增 `GET /openapi/v1/robot/sn`，按 productId 查询设备 SN
	- `OpenMapApi`：新增 `GET /openapi/v2/place/marker/listAll`，取当前使用地图的全量点位，支持楼层过滤和坐标剔除
	- `mvn clean compile` 通过，commit `4a4c720`

- [x] **[lot/open-yuncang-api · 日志分析]** 分析 `rw-open-yuncang-api` 生产日志
	- 定位到 `UboxVmServiceImpl.sendGoodsMsgNew()` → `POST /generalapi/typeOutProduct`（Ubox 第三方接口）
	- 关键发现：`out_type` 由入参 `robotSN` 是否存在决定
		- `robotSN` 为空或以 `fake` 开头 → `out_type=101`
		- `robotSN` 有真实值 → `out_type=102`
	- 本次日志请求体无 `robotSN` 字段 → 走 `out_type=101`；Ubox 返回 200 但货物未出
	- **待验证**：`out_type=101` vs `102` 在 Ubox 侧的实际行为差异，需查 Ubox API 文档确认
	- 梳理了 `POST /api/v2/yuncang/order/unifiedDispatching` 完整入参（Goods 对象字段）

## 学到的东西

## 明日待做

## 今日总结
- **iot-min/open-api**：移植 `GET /openapi/v1/robot/sn`、`GET /openapi/v2/place/marker/listAll`，`/openapi/v1/store/device/query` 因缺 HDOS 配置暂不移植
- **lot/open-yuncang-api**：排查下货未执行原因，定位到 `robotSN` 缺失导致 `out_type=101`，Ubox 侧语义待文档确认
